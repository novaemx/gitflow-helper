# Release 0.5.42

**Date:** 2026-04-22

## What's New

- Add changelog TL;DR guardrail for release/hotfix branches
- Squash(feature): cli-log-file
- Squash(feature): tui-commit-action
- Squash(feature): start-release-debug-logging
- Merge feature 'remove-choco-add-linux-aarch64' into develop
- Squash(feature): auto-detect-protected-branch-dirty
- Squash(feature): update-gitflow-skill-file-scope-policy
- Add gitflow protection policy and violation analysis
- Merge feature 'add-author-version-output' into develop
- Squash(feature): add author information to version output
- Squash(feature): tui-fluid-activity-release-list
- Squash(feature): homebrew-formula-completions
- Squash(feature): publish-changelog-executive-summary

## Bug Fixes

- Handle tag-behind-head with ephemeral clone build
- Squash(bugfix): enhance release process with branch checks and tag validation
- Squash(bugfix): improve BUILD_REF inference and error handling in release process
- Squash(bugfix): move-current-changes-bugfix
- Squash(bugfix): scode-cursor-detection

